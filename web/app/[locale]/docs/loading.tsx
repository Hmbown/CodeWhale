import { LoadingRoute } from "@/components/route-state";

/** Docs-segment loading boundary: the shell stays, the article streams. */
export default function DocsLoading() {
  return <LoadingRoute />;
}
